import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.novapowerslandingpagemarch2026',
  appName: 'Nova Powers Landing Page March 2026',
  webDir: 'dist'
};

export default config;
